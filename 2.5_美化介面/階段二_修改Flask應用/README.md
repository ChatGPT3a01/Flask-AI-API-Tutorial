# 2.5 美化介面 - 階段二：修改 Flask 應用

## 學習目標
- 使用 `render_template` 提供 HTML 頁面
- 加入 JavaScript 處理使用者互動
- 使用 fetch API 發送非同步請求

## 與階段一的差異
- app.py 首頁路由改用 `render_template('index.html')`
- index.html 加入完整的 JavaScript 功能
- 可以實際與 AI 對話

## 執行方式

```bash
# 啟動虛擬環境
cd AI_Projects
venv\Scripts\activate

# 進入此資料夾執行
cd "2.5_美化介面/階段二_修改Flask應用"
python app.py

# 開啟瀏覽器
# http://localhost:5000/
```
